/* style.css */

/* 全体設定 */
body {
    font-family: 'Arial', sans-serif;
    background-color: #e8f4f8; /* 薄い青の背景色 */
    color: #333;
    margin: 0;
    padding: 20px;
    text-align: center;
}

/* コンテンツを囲むメインボックス */
.container {
    max-width: 800px;
    margin: 0 auto;
    background-color: #ffffff; /* 白い背景 */
    padding: 30px;
    border-radius: 12px; /* 角を丸くする */
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15); /* 影をつけて立体的に */
}

/* メインタイトル */
h1 {
    color: #007bff; /* 青色 */
    border-bottom: 3px solid #007bff;
    padding-bottom: 15px;
    margin-bottom: 25px;
    font-size: 2.2em;
}

/* セクションタイトル */
h2 {
    color: #28a745; /* 緑色 */
    margin-top: 35px;
    padding-bottom: 5px;
    border-left: 5px solid #28a745;
    padding-left: 10px;
    text-align: left;
}

/* 段落 */
p {
    line-height: 1.7;
    text-align: left;
    padding-left: 15px;
}

/* リスト */
ul {
    list-style-type: none; /* デフォルトのマーカーを消す */
    text-align: left;
    padding-left: 15px;
}

ul li {
    background: #e9ecef; /* リスト項目の背景色 */
    margin-bottom: 8px;
    padding: 10px;
    border-radius: 5px;
    box-shadow: 1px 1px 3px rgba(0, 0, 0, 0.05);
}

/* 区切り線 */
hr {
    border: 0;
    height: 1px;
    background: #ccc;
    margin: 30px 0;
}

/* フッター */
footer {
    margin-top: 40px;
    padding-top: 20px;
    border-top: 1px solid #ddd;
    color: #6c757d;
}
